




.onAttach <- function(...) {

  # if (interactive()) {
  #   m <- "Looks like you cannot run .vbs scripts. See ?use_tools_interactively"
  #   if (can_call_vbs()) {
  #     m <- "Looks like you can run .vbs scripts. See ?use_tools_automatically"
  #   }
  #   packageStartupMessage(m)
  #
  # }
  # invisible(NULL)
}





